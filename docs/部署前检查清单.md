# 部署前检查清单

## 环境变量

- [ ] `.env` 已在部署环境配置
- [ ] `.env` 未提交、未打包
- [ ] `ENABLE_DEBUG_OUTPUT=false`
- [ ] `ENABLE_PROMPT_DEBUG` 按生产策略设置
- [ ] `PRIVACY_CONTACT_EMAIL` 已配置

## 数据库

- [ ] `DATABASE_URL` 已配置为生产数据库
- [ ] 已安装对应数据库驱动
- [ ] 已准备备份与恢复方案
- [ ] 已清理本地测试数据库

## 对象存储

- [ ] `STORAGE_PROVIDER` 已配置
- [ ] Bucket / Endpoint / Region 已配置
- [ ] 文件 public URL 可访问
- [ ] 上传大小和类型限制符合业务要求

## 域名与 CORS

- [ ] 已配置 HTTPS 域名
- [ ] `CORS_ALLOW_ORIGINS` 已设置真实 H5 / 小程序后台域名
- [ ] 不使用生产环境全开放 CORS

## 登录与合规

- [ ] 已接入正式微信登录
- [ ] 已配置微信小程序合法域名
- [ ] 已准备用户协议
- [ ] 已准备隐私协议
- [ ] 已准备内容安全策略

## 内容安全

- [ ] 已评估是否接入真实内容审核服务
- [ ] 已验证自伤、违法、色情、隐私、Prompt 注入处理
- [ ] 已关闭面向用户的敏感 debug 输出

## 数据隔离

- [ ] 已确认 user_id 隔离优先于 project_id
- [ ] 已关闭或迁移 legacy `user_id=null` 数据
- [ ] 已清理测试用户、测试项目、测试上传文件

## 自动化测试

- [ ] `python tests/run_backend_smoke.py` 通过
- [ ] `python tests/run_project_isolation.py` 通过
- [ ] `python tests/run_safety_checks.py` 通过
- [ ] `npm run build:h5` 通过
- [ ] `npm run build:mp-weixin` 通过
- [ ] `python tests/run_package_check.py --zip <zip>` 通过

## 压缩包安全

- [ ] 不包含 `.env`
- [ ] 不包含真实密钥
- [ ] 不包含 `backend/fantasy_role_ai.db`
- [ ] 不包含真实上传文件
- [ ] 不包含 `frontend/node_modules/`
- [ ] 不包含 `frontend/dist/`
- [ ] 不包含 `frontend/unpackage/dist/`
- [ ] 不包含 `tests/reports/`
- [ ] 不包含 `.git/`
- [ ] 不包含 `.idea/`、`.vscode/`
